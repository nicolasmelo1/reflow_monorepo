/** @module src/authentication/events */

const { UserConsumer } = require('../core/consumers')
const { User } = require('./models')


class AuthenticationBroadcastEvent {

}

module.exports = {
    AuthenticationBroadcastEvent
}